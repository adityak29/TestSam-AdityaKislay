
@{
    ViewBag.Title = "Index";
}
<html>

<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>

</head>
<body>
    @using System.Diagnostics    @using FizzBuzz.Models;
@using StructureMap.Query
    @model FizzBuzz.Models.FizzBuzzInput
    @using (Html.BeginForm("FizzBuzzView", "Home", FormMethod.Post))
    {
        <span>Enter the digit</span>
        @Html.TextBoxFor(Model => Model.num, new { maxlength = "100" })
        <input type="submit" value="Submit" onclick="" />
    }
@if (Model != null)
{
    foreach (var item in FizzBuzzOutput)
    {

    }
}
foreach
    @if (TempData["Message"] != null)
    {
        var i = TempData["Message"];
       
         // @foreach( var item in ) { }
        <script type="text/javascript">
        window.onload = function() { alert('@i'); };
        </script>
    }

    <h2>FizzBuzzView</h2>

</body>
</html>